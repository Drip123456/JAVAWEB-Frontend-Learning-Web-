
var rows = document.getElementsByClassName("tab");
for (let i = 0; i < rows.length; i++) {
    var tr = rows[i];
    var cells = tr.cells;
    var name = cells[1];
    var img = cells[4];
    if (img && img.tagName == "IMG") {

    }

}